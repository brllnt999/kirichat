import { AuthSession } from '@/lib/auth/utils'
import React from 'react'

const InlineUserButton = ({session}:AuthSession) => {
  return (
    <div>
        
    </div>
  )
}

export default InlineUserButton